// This file can be replaced during build by using 'fileReplacements' array.
// `ng-build --prod` 
// replaces `environment.ts` with `environment.prod.ts`.
// The list of file replaces can be found in `angular.json`.
export const environment = {
    production: false,
    backendUrl: 'http://localhost:3000'
};